name: Production CI

on:
  workflow_call:
    secrets:
      GPG_PRIVATE_KEY:
        required: false
      GPG_PASSPHRASE:
        required: false

jobs:
  # Job 1: Manual/Initial Confirmation
  confirm:
    name: ✅ Confirmation
    runs-on: ubuntu-latest
    steps:
      - name: Confirm
        run: echo "Release production confirmed."

  # Job 2: Commit Lint Check
  call_commitlint:
    name: ⚙️ Commit Lint Check
    uses: ./.github/workflows/commitlint.yaml
    secrets: inherit

  # Job 3: Markdown Lint Check
  call_markdownlint:
    name: 📄 Markdown Lint Check
    uses: ./.github/workflows/markdownlint.yaml
    secrets: inherit
    
  # Job 4: Smoke Tests Call
  call_smoke_tests:
    name: 💨 Run Smoke Tests
    uses: ./.github/workflows/smoke-tests.yaml
    secrets: inherit

## Job 5: Core Environment Steps
  production_ci:
    name: Production Build Steps
    runs-on: ubuntu-latest
    needs: [confirm, call_commitlint, call_markdownlint, call_smoke_tests]
    environment: production
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: 🛠️ Report Successful production Call
        run: |
          echo "Processing production build..."

  # Job 6: Semantic Release
  call_semantic_release:
    needs: production_ci
    uses: ./.github/workflows/semantic-release.yaml
    with:
      environment: production # <--- Pass the required input here
    secrets: inherit
