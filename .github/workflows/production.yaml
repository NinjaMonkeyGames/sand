name: Production Deployment

on:
  workflow_call:
    # This enables the "call" from your CI Controller

jobs:
  deploy-dev:
    name: Execute Production Build
    runs-on: ubuntu-latest
    # This links the job to a GitHub Environment for tracking
    environment: production

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Production Simulation
        run: |
          echo "-------------------------------------------------------"
          echo "🚀 STARTING PRODUCTION DEPLOYMENT"
          echo "-------------------------------------------------------"
          echo "Target Environment: Production"
          echo "Triggered by: ${{ github.actor }}"
          echo "Status: In Progress..."
          
          # Simulate work
          sleep 2
          
          echo "✅ Production deployment marked as COMPLETE."
          echo "-------------------------------------------------------"

      - name: Final Success Message
        run: echo "The production workflow has finished successfully."