on:
  workflow_call:

jobs:
  confirm:
    runs-on: ubuntu-latest
    steps:
      - name: job
        run: echo "Staging deployment confirmed."

      - name: Confirm
        run: echo "Staging deployment confirmed."

    environment: staging