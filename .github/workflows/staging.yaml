name: Staging CI

on:
  workflow_call:
    secrets:
      GPG_PRIVATE_KEY:
        required: false
      GPG_PASSPHRASE:
        required: false
      APP_ID:
        required: true
      APP_PRIVATE_KEY:
        required: true
      SSH_SIGNING_KEY:
        required: true
jobs:
  # Job 1: Manual/Initial Confirmation
  confirm:
    name: ✅ Confirmation
    runs-on: ubuntu-latest
    steps:
      - name: Confirm
        run: echo "Release staging confirmed."

  # Job 2: Commit Lint Check
  call_commitlint:
    name: ⚙️ Commit Lint Check
    uses: ./.github/workflows/commitlint.yaml
    secrets: inherit

  # Job 3: Markdown Lint Check
  call_markdownlint:
    name: 📄 Markdown Lint Check
    uses: ./.github/workflows/markdownlint.yaml
    secrets: inherit
    
  # Job 4: Smoke Tests Call
  call_smoke_tests:
    name: 💨 Run Smoke Tests
    uses: ./.github/workflows/smoke-tests.yaml
    secrets: inherit
  # Job 4: NEW JOB - Smoke Tests Call
    
  call_semantic_release:
    name: 💨 Run Semantic Release
    needs: [call_commitlint, call_markdownlint, call_smoke_tests]
    uses: ./.github/workflows/semantic-release.yaml
    secrets: inherit

  # Job 5: Core Environment Steps
  staging_ci:
    name: Staging Build Steps
    runs-on: ubuntu-latest
    # This job waits for all linting, smoke tests, AND the confirmation job
    needs: [confirm, call_commitlint, call_markdownlint, call_smoke_tests, call_semantic_release]
    environment: staging
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: 🛠️ Report Successful Staging Call
        run: |
          echo "#####################################################"
          echo "## STAGING WORKFLOW (Staging Branch) CALLED!   ##"
          echo "#####################################################"
          echo "All linting and smoke tests passed. Proceeding with core staging build."