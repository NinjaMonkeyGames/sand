name: Staging Deployment

on:
  workflow_call:
    # Receives the call from CI Controller

jobs:
  # 1. Linting Jobs (Running in parallel)
  commit-lint:
    uses: ./.github/workflows/commitlint.yaml
    secrets: inherit

  markdown-lint:
    uses: ./.github/workflows/markdownlint.yaml
    secrets: inherit

  # 2. Deployment Job
  deploy-staging:
    name: Execute Staging Build
    needs: [commit-lint, markdown-lint]
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deployment Simulation
        run: |
          echo "-------------------------------------------------------"
          echo "🚀 STARTING STAGING DEPLOYMENT"
          echo "-------------------------------------------------------"
          echo "✅ Staging deployment marked as COMPLETE."
          echo "-------------------------------------------------------"

  # 3. Release Job (Triggers only if lints passed)
  release:
    name: Semantic Release
    needs: [commit-lint, markdown-lint]
    uses: ./.github/workflows/semantic-release.yaml
    secrets: inherit