name: Deployment # (Update the name per file)

on:
  workflow_call:
    inputs:
      env_name: # This tells the workflow to expect the variable from ci.yaml
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.env_name }} # This is where the magic happens
    steps:
      - name: Deploy
        run: echo "🚀 Successfully deployed to the ${{ inputs.env_name }} environment!"